# 🎬 WETH Guardian — 90-Second Hackathon Demo Video Script & Shot List

> **Target Length:** 1 minute 30 seconds (90 seconds)  
> **Live Frontend URL:** `https://weth-asp-web.vercel.app/`  
> **Live Backend URL:** `https://weth-asp-api.onrender.com/`  
> **OKX ASP Agent ID:** `#6351` (`1 USDT` per call)  

---

## 🛠️ Pre-Recording Setup (1 Minute Before Hit Record)
1. **Browser Preparation:**
   - Open your browser in full screen (1080p / 1920x1080 resolution recommended).
   - Go to **`https://weth-asp-web.vercel.app/dashboard`**.
   - Close unnecessary browser extensions or extra tabs so the monochromatic UI pops cleanly.
2. **Audio Check:**
   - Speak clearly, energetically, and with a confident tone.
   - If you prefer not to use voiceover while clicking, you can record your screen actions first using the shot list below, and record the voiceover on top!

---

## ⏱️ Second-by-Second Script & Screen Actions

### 🟢 Act 1: The Hook & Core Innovation (0:00 – 0:15 | 15 Seconds)
* **Visual Action on Screen:**
  - Start right on **`https://weth-asp-web.vercel.app/dashboard`**.
  - Hover over the header badge `[x402_PAYMENT_MODE: ACTIVE]` and click between the chain pills (`[ETH_MAINNET]`, `[BASE]`, `[ARBITRUM]`).
* **Word-for-Word Voiceover:**
  > *"Most Web3 AI security tools today do only one thing: when an agent intercepts a transaction asking for infinite token approvals, they block it. But blocking trades kills AI automation.*
  > 
  > *Today we present **WETH Guardian**—an autonomous A2MCP Agent Service Provider registered on OKX.AI as Agent `#6351` that doesn't just block dangerous transactions... it auto-remediates them on the fly."*

---

### 🟢 Act 2: The Command Center & 100/100 Safety Index (0:15 – 0:35 | 20 Seconds)
* **Visual Action on Screen:**
  - Point your mouse to the circular **Safety Score Gauge** (`SAFETY_INDEX: 100/100` with the full green ring and `[VERIFIED_SAFE]` badge).
  - Move your cursor to the search bar (`Enter Wallet Address...`) and click the white **`[EXECUTE LIVE SCAN]`** button on the right side.
  - Scroll down slightly to show the real-time entry added immediately at the top of the **Institutional Audit Activity Log** table at the bottom of the page (`Target: 0x742d...f44e | Verdict: SAFE`).
* **Word-for-Word Voiceover:**
  > *"Here on our live Vercel dashboard, our engine calculates a real-time **Safety Index out of 100** by traversing multi-hop Neo4j threat graphs and scanning active ERC-20 token allowances.*
  > 
  > *When we execute a live scan, our system verifies wallet posture across Ethereum, Base, and Arbitrum in under 45 milliseconds and logs institutional-grade audit receipts directly to the activity ledger."*

---

### 🔴 Act 3: The Hero Demo — Smart Calldata Sanitizer (0:35 – 1:10 | 35 Seconds)
* **Visual Action on Screen:**
  - Click **`TRANSACTIONS`** in the top navigation bar (`/transactions`).
  - Point to the **Calldata (Hex)** input box pre-loaded with `0x095ea7b3000...ffffffffffffffff` (the classic infinite token approval payload).
  - Click the white **`EXECUTE SIMULATION`** button right below the calldata box.
  - **Show the Split-Screen Reaction:**
    - Highlight the circular Risk Gauge on the left dropping immediately to **`19 / 100` in bright red** (`[CRITICAL_EXPOSURE // REJECT]`).
    - Highlight the high-contrast green hero box on the right: **`[AUTO-REMEDIATION // SANITIZED CALLDATA GENERATED]`**.
    - Point out the newly generated safe hex string inside the box (`0x095ea7b3...00000100`).
  - Click the white **`[COPY SANITIZED PAYLOAD]`** button inside the green box so the text flashes **`[COPIED!]`**!
* **Word-for-Word Voiceover:**
  > *"Now watch what happens when an AI agent or user attempts to sign an ERC-20 token approval with unlimited calldata (`ffff...`).*
  > 
  > *We hit **Execute Simulation**.*
  > 
  > *Instantly, our endpoint catches the critical exposure, dropping the safety score to **19 out of 100 in red** and issuing a `REJECT` recommendation.*
  > 
  > *But instead of stopping your workflow, our **Smart Calldata Sanitizer** automatically intercepts the hex payload, strips out the infinite drain allowance, and generates a sanitized replacement calldata (`...000100`) capped to your exact transaction needs.*
  > 
  > *With one click, you copy the sanitized payload and execute your trade safely without getting drained."*

---

### 🟢 Act 4: Live on OKX Onchain OS Marketplace (`#6351`) & Outro (1:10 – 1:30 | 20 Seconds)
* **Visual Action on Screen:**
  - Switch back to `/dashboard` (or show the top header `[x402_PAYMENT_MODE: ACTIVE]`).
  - *(Optional: If you want, you can show your terminal or a slide highlighting Agent `#6351` (`1 USDT` per call) and `weth-asp-web.vercel.app`).*
* **Word-for-Word Voiceover:**
  > *"WETH Guardian is live right now on Vercel (`weth-asp-web.vercel.app`), powered by our ultra-low-latency Render backend (`weth-asp-api.onrender.com`).*
  > 
  > *Even more importantly, we are officially registered and published on the **OKX Onchain OS AI Marketplace** under Agent ID **`#6351`**, complete with **`1 USDT` pay-per-call A2MCP pricing** and x402 escrow simulation.*
  > 
  > *Don't sign blind—auto-remediate with WETH Guardian. Thank you!"*

---

## 📋 Quick Cheat-Sheet for X (Twitter) Post Caption

```text
🔥 Don't Sign Blind. Introducing WETH Guardian — An Autonomous AI Security Copilot & ASP for @OKX.AI

Most Web3 AI agents just block malicious transactions when protocols ask for infinite token approvals. We built something legendary: Guardrail Auto-Remediation (`Smart Calldata Sanitizer`).

🛡️ What happens when your agent intercepts bad calldata (`0x095ea7b3...ffff`):
1️⃣ Intercepts & rejects infinite drain risk in < 45ms
2️⃣ Auto-remediates the hex payload on-the-fly
3️⃣ Generates a sanitized replacement calldata (`...000100`) capped to safe limits so your trade completes safely without getting drained.

⚡ Live features:
• A2MCP Agent Service Provider (#6351) registered on OKX.AI Marketplace (1 USDT / call)
• x402 Pay-Per-Call Micropayment & Escrow simulation ready
• Multi-hop Threat Graph & local 0ms semantic intent routing
• Multi-chain EVM simulation (Ethereum, Base, Arbitrum)

Try the live $100K monochromatic dashboard: https://weth-asp-web.vercel.app/
OKX ASP Agent ID: #6351

Built for the @OKX x Onchain OS AI & Finance Hackathon 🏁🚀
#Web3 #AIAgents #OKX #Hackathon #EVM #Security
```
